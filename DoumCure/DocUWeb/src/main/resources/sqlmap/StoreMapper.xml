<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
	PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.team3.store.mapper.StoreMapper">

	<select id="getSi" resultType="String">
		SELECT si FROM store GROUP by si order by si asc
	</select>
	
	<select id="getGu" resultType="String">
		SELECT gu FROM store WHERE si = #{si} GROUP by gu order by gu asc
	</select>
	
	<select id="getRo" resultType="String">
		SELECT ro FROM store WHERE gu = #{gu} GROUP by ro order by ro asc
	</select>

</mapper>